#
# ! Do not forget running `make build-lock-files` after updating dependency list !
#
tenacity==9.0.0
msal==1.32.3
azure-core==1.38.0
# Fixing the version to solve known vulnerabilities
requests==2.32.4 # when updating also update in all files
PyJWT==2.12.0 # pinning because msal uses a version with known vulnerabilities
cryptography==46.0.6 # pinning the version to avoid vulnerabilities